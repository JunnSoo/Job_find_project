spring:
  application:
    name: it_job
  datasource:
    url: ${URL_DATABASE}
    username: ${ROOT}
    password: ${PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
upload:
  image: ${PATHFILE}

